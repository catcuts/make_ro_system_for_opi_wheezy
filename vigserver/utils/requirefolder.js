/*　China Fujian Huanyutong Technology Co., Ltd. */
const fs=require("fs"),path=require("path");function requireFolder(e,r="js"){let t=[];if(r=r.split(",").map(e=>"."===e.toLowerCase().charAt(0)?e.toLowerCase():"."+e.toLowerCase()),fs.existsSync(e))for(let a of fs.readdirSync(e))try{let o=path.join(e,a);if(r.includes(path.extname(o)))try{t.push(require(o))}catch(e){logger.warn(_("Cannot require module <{name}>:{err}").params(a,e.stack))}}catch(e){logger.warn(_("Cannot require module from {name}:{err}").params(o,e.stack))}return t}module.exports={requireFolder:requireFolder};